# Invoiss POS Environment Configuration

# API Mode
# Options: 'mock' (development) | 'dev' (staging) | 'prod' (production)
# - mock: Uses local mock API, no Clover sync
# - dev: Uses Invoiss staging API, syncs with test Clover account
# - prod: Uses Invoiss production API, syncs with live Clover account
VITE_API_MODE=mock

# Invoiss API Key
# Get your API key from: https://dashboard.invoiss.com
# Leave empty for mock mode
# When set to 'dev' or 'prod', ALL orders, payments, and customers sync with Clover
VITE_INVOISS_API_KEY=

# Optional: Clover Merchant ID
# Only needed if using direct Clover integration
VITE_CLOVER_MERCHANT_ID=

# Supabase (for delivery management, photos, etc.)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
